# Legacy homepage

`index.html` is the final cleaned static homepage from phase one. It is retained
only as a reference for the earlier layout and content.

The active site now uses framework-free HTML and CSS from the repository root.
The redesigned homepage is `../index.html`, with its styles in `../css/home.css`.
The family and pet pages remain native static pages alongside it.

